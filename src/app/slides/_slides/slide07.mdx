import { Notes } from '../_components/notes.tsx'
import { MainSlide } from './slide-layouts.tsx'

# Pair up with someone

<Notes>
Since the above might not be the area of expertise that you specialize in, pairing up with someone might be a good idea. Preferably someone that complements you and also can help challenge what you want to do to check if it even make sense.
</Notes>

export default ({children}) => {
  return <MainSlide>
    {children}
  </MainSlide>
}