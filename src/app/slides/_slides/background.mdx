import Image from "next/image";
import { ImageSwitch } from "../_components/image-switch";
import { Notes } from "../_components/notes";
import { Column, TwoColumns } from "../_components/slide-layouts";
import Comment1 from "./images/bad_comment1.png";
import Comment2 from "./images/bad_comment2.png";
import MissingPrices from "./images/missing_prices.png";
import PageSpeed from "./images/pagespeed.png";
import SpeedInsights from "./images/speed_insights.png";

<TwoColumns>
    <Column title="From this">
      <div className="relative grid grid-rows-[40%_1fr_1fr] h-3/4 min-h-0 overflow-hidden">
        <div className="mx-auto flex flex-row justify-center min-h-0">
          <Image
            src={MissingPrices}
            alt="Missing price sample"
            className="object-contain min-h-0"
          />
        </div>
        <div className="flex flex-row justify-center mx-auto">
          <Image
            src={Comment1}
            alt="Bad comment example 1"
            className="object-contain"
          />
        </div>
        <div className="flex flex-row justify-center mx-auto">
          <Image
            src={Comment2}
            alt="Bad comment example 2"
            className="object-contain"
          />
        </div>
      </div>
    </Column>
    <Column title="To this">
      <div className="mx-auto flex flex-row justify-center">
        <ImageSwitch
          images={[
            <Image
              key={SpeedInsights.src}
              src={SpeedInsights}
              alt="Current speed insights"
            />,
            <Image
              key={PageSpeed.src}
              src={PageSpeed}
              alt="Current page speed"
            />,
          ]}
        />
      </div>
    </Column>
</TwoColumns>

{
  /* <Column title={titleColumn1}>{column1}</Column>
<Column title={titleColumn2}>{column2}</Column> */
}

  <Notes>
    If you have used any of our sites in Norway, Sweden, Denmark or Finland the last couple of years you might have experienced the site looking something like the image on the left. What you see is a screenshot from the old site where the prices are missing. That could of course be fine, if they stream in within a reasonable time frame. In this particular example when I took the screenshot we had reports it took 30 seconds or more for the prices to load. I don't know about you, but I don't have that long patience and would leave any site that has that kind of experience, unless I know there is a really great offer there... then I might suffer through it.

    This experience also lead to some comments online, like the ones below the image. The first one is translated to something like "The site is horrible, so I end up on another site". The second one is from a person working in one of our stores and they are also frustrated. These are just two examples of many comments online the last couple of years. As a developer it sort of hurts seeing these kind of feedback, even though I was not part of the team delivering the previous version, so that is something I wanted to address. I even had a fellow speaker here at the conference asked me when I joined elkjøp when I will fix the site. So this is partly the story of how we made it from what you see on the left to what we have one the right hand side. The score on the right is from Vercel's speed insights and we also have the Google's page speed insights, and this score has been consistent since we went live.

  </Notes>