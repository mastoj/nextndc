import { Notes } from '../_components/notes.tsx'
import { MainSlide } from './slide-layouts.tsx'

# Tell AND show

<Notes>
Make a presentation that you can point to and present. Also, if you can make a quick PoC of some part of what will happen will also make it easier to pitch what you want to do. In our case we had the complete base already finished, but that is only a small part of the job in total.
</Notes>

export default ({children}) => {
  return <MainSlide>
    {children}
  </MainSlide>
}