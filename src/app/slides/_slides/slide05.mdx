import { Notes } from '../_components/notes.tsx'
import { MainSlide } from './slide-layouts.tsx'

# Don't sell the tech, sell the problem

<Notes>
First point is what I call "sell the problem". Often as developer we tend to focus on the tech much more than the problem we are going to solve. Focusing on the problem and why it is a problem is definitely more effective to get an approval from management. 
</Notes>

export default ({children}) => {
  return <MainSlide>
    {children}
  </MainSlide>
}